export interface Company {
  id: string;
  name: string;
  category: string;
  authorizedPerson: string;
  phone: string;
  whatsapp: string;
  address: string;
  district: string;
  website: string;
  description: string;
  logo: string;
  isApproved: boolean | number;
  createdAt: string;
}
